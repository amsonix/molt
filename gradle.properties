org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true

moltVersion=1.1.0

# Optional: publish to Nexus (override via env or ~/.gradle/gradle.properties)
# NEXUS_USERNAME=
# NEXUS_PASSWORD=
