org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true

# 本机 JDK 17 路径请复制 gradle.properties.local.example → gradle.properties.local（已 gitignore）。
# Gradle 8.0 需 JDK 17；切换后建议：./gradlew --stop
