org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true

# 本机 JDK 路径请复制 gradle.properties.local.example → gradle.properties.local（已 gitignore）。
# 切换 JDK 或 Gradle 版本后建议：./gradlew --stop
